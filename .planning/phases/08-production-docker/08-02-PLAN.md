---
phase: 08-production-docker
plan: 02
type: execute
wave: 2
depends_on:
  - '08-01'
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - DOCK-02
  - DOCK-03
  - DOCK-04

must_haves:
  truths:
    - 'CLAUDE.md Conventions section contains verified patterns from phases 2-7'
    - 'CLAUDE.md documents Docker build and production deployment commands'
    - 'CLAUDE.md folder structure includes docker/ directory'
    - 'CLAUDE.md documents all pnpm run scripts accurately'
  artifacts:
    - path: 'CLAUDE.md'
      provides: 'Finalized project documentation with conventions, architecture, and Docker commands'
      contains: 'docker/Dockerfile'
  key_links:
    - from: 'CLAUDE.md'
      to: 'docker/Dockerfile'
      via: 'documented build command'
      pattern: 'docker build -f docker/Dockerfile'
---

<objective>
Perform a full audit update of CLAUDE.md to reflect the finalized codebase state after all 8 phases of development. This is per D-12: not just adding Docker commands, but filling in the Conventions and Architecture sections that currently say "not yet established" with verified patterns from the completed project.

Purpose: CLAUDE.md is the primary context file for AI coding agents. After 7 phases of development, real conventions and architecture patterns exist but are not documented here. This update ensures any developer or AI agent cloning this repo gets accurate, complete project context.

Output: Updated `CLAUDE.md` with populated Conventions section, Architecture section, Docker commands, and verified script documentation.
</objective>

<execution_context>
@/workspaces/sveltekit-starter/.claude/get-shit-done/workflows/execute-plan.md
@/workspaces/sveltekit-starter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-production-docker/08-CONTEXT.md
@.planning/phases/08-production-docker/08-RESEARCH.md
@.planning/phases/08-production-docker/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full audit update of CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (entire current content — understand structure and GSD comment markers)
    - package.json (verify all scripts are documented accurately)
    - docker/Dockerfile (just created — document build command)
    - docker/docker-compose.prod.yml (just created — document deployment command)
    - docker-compose.yml (dev compose — verify documentation is accurate)
    - .env.example (verify env var documentation)
    - src/lib/server/env/schema.ts (env validation pattern)
    - src/lib/server/db/index.ts (DB client singleton pattern)
    - src/lib/server/logger.ts (structured logging pattern)
    - src/hooks.server.ts (hooks pipeline pattern with sequence())
    - src/instrumentation.server.ts (OTEL initialization pattern)
    - svelte.config.js (adapter-node + CSP config)
    - src/app.css (CSS-first Tailwind + DaisyUI config)
    - src/routes/+layout.svelte (Svelte 5 runes pattern)
    - eslint.config.js (flat config pattern)
    - knip.config.ts (dead code detection config)
    - drizzle.config.ts (Drizzle config pattern)
    - .planning/phases/08-production-docker/08-RESEARCH.md (verified conventions list in Pitfall 6 section)
  </read_first>
  <action>
Update CLAUDE.md by modifying content WITHIN the existing GSD comment markers. Do NOT remove or alter the GSD markers themselves (`<!-- GSD:*-start -->` and `<!-- GSD:*-end -->`). Do NOT reorganize the overall structure. Keep all existing sections; only fill gaps and add missing items.

**Section 1: Update the "Conventions" section** (between `<!-- GSD:conventions-start -->` and `<!-- GSD:conventions-end -->`).

Replace "Conventions not yet established. Will populate as patterns emerge during development." with the following verified conventions:

```markdown
## Conventions

### Svelte 5 Runes (NOT Stores)

- Use `$state`, `$derived`, `$effect`, `$props` for all reactivity
- Use `{@render children()}` for slot content — NOT `<slot />`
- Do NOT use `writable()`, `readable()`, or `derived()` stores from `svelte/store`

### Server-Only Boundary

- All server-only code lives in `$lib/server/` — importing from this path in client code causes a build error
- Database client, env validation, logger, and all secrets are in `$lib/server/`
- Use `$env/dynamic/private` for secrets (NOT `$env/static/private`) to support Docker runtime env vars

### Environment Validation

- Zod schema in `src/lib/server/env/schema.ts` validates all required env vars at startup
- App fails fast with a clear Zod error if a required variable is missing
- Import validated env as `import { env } from '$lib/server/env'`

### Database

- Drizzle ORM with `pg` (node-postgres) driver — NOT `postgres.js`
- DB client singleton in `src/lib/server/db/index.ts` (module-level Pool, not per-request)
- Schema files in `src/lib/server/db/schema/`
- Migrations committed to `drizzle/` directory (auto-generated names, do not rename)
- Dev workflow: `db:push` to validate schema, then `db:generate` to produce migration SQL

### Logging

- Structured JSON logging via pino (`src/lib/server/logger.ts`)
- Use `logger.info(...)`, `logger.error(...)` — NOT `console.log`
- Log level controlled by `LOG_LEVEL` env var

### Hooks Pipeline

- `src/hooks.server.ts` uses `sequence()` to compose handle functions
- Order: nosecone security headers first, then OTEL enrichment, then logging/locals
- Each handle function is a named export for testability

### CSS-First Styling

- All Tailwind + DaisyUI config in `src/app.css` via `@import` and `@plugin` — no `tailwind.config.js`
- DaisyUI themes set via `data-theme` attribute on `<html>` element

### Code Quality

- ESLint flat config (`eslint.config.js`) — no `.eslintrc` files
- Prettier plugin order: `prettier-plugin-svelte` THEN `prettier-plugin-tailwindcss` (tailwind last)
- Pre-commit hook via Husky + lint-staged runs ESLint + Prettier on staged files
- Knip for dead code detection with SvelteKit-aware config
```

**Section 2: Update the "Architecture" section** (between `<!-- GSD:architecture-start -->` and `<!-- GSD:architecture-end -->`).

Replace "Architecture not yet mapped. Follow existing patterns found in the codebase." with:

```markdown
## Architecture

### Folder Structure
```

src/
app.css # Tailwind v4 + DaisyUI v5 CSS-first config
app.html # HTML shell with data-theme
hooks.server.ts # Request pipeline: security -> OTEL -> logging
instrumentation.server.ts # OTEL SDK init (runs before any app code)
lib/
server/
db/
index.ts # Drizzle client + pg Pool singleton
schema/ # Drizzle schema files (posts.ts, etc.)
env/
index.ts # Validated env export
schema.ts # Zod env validation schema
logger.ts # Pino structured logger
routes/
+layout.svelte # Root layout (navbar + drawer + theme toggle)
+page.svelte # Home page
+error.svelte # Error page
api/
health/
+server.ts # GET /api/health (DB connectivity check)
docker/
Dockerfile # Multi-stage production build (3 stages)
docker-compose.prod.yml # Production: app + postgres
docker-compose.yml # Dev only: postgres + Aspire Dashboard
drizzle/ # Committed migration SQL files
meta/ # Migration journal + snapshots

```

### Key Commands

| Command | Purpose |
|---------|---------|
| `pnpm run dev` | Start SvelteKit dev server (hot reload) |
| `pnpm run build` | Build for production (adapter-node output in `build/`) |
| `pnpm run preview` | Preview production build locally |
| `pnpm run check` | Run svelte-check TypeScript validation |
| `pnpm run lint` | Run ESLint |
| `pnpm run format` | Run Prettier |
| `pnpm run knip` | Run dead code detection |
| `pnpm run test:unit` | Run Vitest unit + component + integration tests |
| `pnpm run test:e2e` | Run Playwright E2E tests |
| `pnpm run test` | Run all tests (unit + E2E) |
| `pnpm run db:push` | Push schema to dev DB (validates schema) |
| `pnpm run db:generate` | Generate migration SQL from schema diff |
| `pnpm run db:migrate` | Apply migrations to database |
| `pnpm run db:studio` | Open Drizzle Studio UI |
| `docker compose up` | Start dev dependencies (PostgreSQL + Aspire Dashboard) |
| `docker build -f docker/Dockerfile .` | Build production Docker image |
| `docker compose -f docker/docker-compose.prod.yml up` | Run production stack (app + PostgreSQL) |

### Request Flow

1. Request enters SvelteKit
2. `hooks.server.ts` `sequence()` pipeline runs:
   - Nosecone adds security headers (CSP with nonce, HSTS, X-Frame-Options, etc.)
   - OTEL handle enriches the active span with route info
   - Logging handle populates `event.locals`
3. Route handler executes (page or API endpoint)
4. Response includes security headers on every response
```

**Section 3: Verify the "Docker Compose Dev Stack" subsection** under Technology Stack.

The existing content documents ports. Verify it is accurate against the current `docker-compose.yml`. If accurate, leave it. If any port mappings changed, update them.

**Section 4: Verify the "Environment Variables (.env)" subsection.**

Currently it just has `# Database` and `# OpenTelemetry` headers with no content. Add the actual env vars from `.env.example`:

```markdown
## Environment Variables (.env)

### Database

| Variable            | Required | Default                                           | Description                                                                                           |
| ------------------- | -------- | ------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| `POSTGRES_USER`     | Yes      | `postgres`                                        | PostgreSQL username                                                                                   |
| `POSTGRES_PASSWORD` | Yes      | `postgres`                                        | PostgreSQL password                                                                                   |
| `POSTGRES_DB`       | Yes      | `app`                                             | PostgreSQL database name                                                                              |
| `DATABASE_URL`      | Yes      | `postgres://postgres:postgres@localhost:5432/app` | Full connection URL (use service name `postgres` instead of `localhost` in Docker Compose production) |

### OpenTelemetry

| Variable                      | Required | Default                 | Description                   |
| ----------------------------- | -------- | ----------------------- | ----------------------------- |
| `OTEL_EXPORTER_OTLP_ENDPOINT` | Yes      | `http://localhost:4318` | OTLP HTTP endpoint for traces |
| `OTEL_SERVICE_NAME`           | Yes      | `sveltekit-starter`     | Service name in traces        |

### Logging

| Variable    | Required | Default | Description                                        |
| ----------- | -------- | ------- | -------------------------------------------------- |
| `LOG_LEVEL` | No       | `info`  | Pino log level (trace/debug/info/warn/error/fatal) |

### Production

| Variable | Required  | Default | Description                                                          |
| -------- | --------- | ------- | -------------------------------------------------------------------- |
| `ORIGIN` | Prod only | —       | Full origin URL for CSRF validation (e.g., `https://yourdomain.com`) |
| `PORT`   | No        | `3000`  | HTTP port for adapter-node                                           |
```

**IMPORTANT:** Preserve all GSD comment markers exactly as they are. Only modify content between the markers. If a section is not between GSD markers (like Environment Variables), edit it in place.
</action>
<verify>
<automated>grep -q "Svelte 5 Runes" /workspaces/sveltekit-starter/CLAUDE.md && grep -q "docker/Dockerfile" /workspaces/sveltekit-starter/CLAUDE.md && grep -q "pnpm run db:migrate" /workspaces/sveltekit-starter/CLAUDE.md && grep -q "sequence()" /workspaces/sveltekit-starter/CLAUDE.md && grep -q "Folder Structure" /workspaces/sveltekit-starter/CLAUDE.md && grep -q "ORIGIN" /workspaces/sveltekit-starter/CLAUDE.md && ! grep -q "Conventions not yet established" /workspaces/sveltekit-starter/CLAUDE.md && ! grep -q "Architecture not yet mapped" /workspaces/sveltekit-starter/CLAUDE.md && echo "PASS"</automated>
</verify>
<acceptance_criteria> - CLAUDE.md does NOT contain "Conventions not yet established" - CLAUDE.md does NOT contain "Architecture not yet mapped" - CLAUDE.md contains "Svelte 5 Runes" in the Conventions section - CLAUDE.md contains "$lib/server/" boundary documentation
    - CLAUDE.md contains "$env/dynamic/private" convention - CLAUDE.md contains "Drizzle ORM" and "pg" driver in conventions - CLAUDE.md contains "pino" or "logger.ts" in conventions - CLAUDE.md contains "sequence()" in hooks pipeline convention - CLAUDE.md contains "Folder Structure" with `docker/` directory listed - CLAUDE.md contains "Key Commands" table with `docker build -f docker/Dockerfile .` - CLAUDE.md contains "docker compose -f docker/docker-compose.prod.yml" - CLAUDE.md contains `ORIGIN` in the Environment Variables section - CLAUDE.md contains `DATABASE_URL` with description mentioning Docker service name - CLAUDE.md preserves all `<!-- GSD:*-start -->` and `<!-- GSD:*-end -->` comment markers - `grep -c 'GSD:' CLAUDE.md` returns the same count as before the edit (markers preserved)
</acceptance_criteria>
<done> - Conventions section populated with 8 verified convention categories (Svelte 5 runes, server boundary, env validation, database, logging, hooks, CSS-first, code quality) - Architecture section populated with folder structure, key commands table (all pnpm scripts + Docker commands), and request flow description - Environment Variables section populated with all env vars from .env.example plus ORIGIN and PORT - All GSD comment markers preserved intact - No content removed — only gaps filled and missing items added
</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                                        |
| ---------------------- | ---------------------------------------------------------------------------------- |
| CLAUDE.md -> AI agents | Documentation influences AI-generated code; inaccurate docs lead to wrong patterns |

## STRIDE Threat Register

| Threat ID | Category               | Component | Disposition | Mitigation Plan                                                                                     |
| --------- | ---------------------- | --------- | ----------- | --------------------------------------------------------------------------------------------------- |
| T-08-06   | Information Disclosure | CLAUDE.md | mitigate    | Do not document actual secret values; only document env var names and where to find values          |
| T-08-07   | Tampering              | CLAUDE.md | accept      | CLAUDE.md is version-controlled; changes are reviewed via git diff; low risk for a starter template |

</threat_model>

<verification>
1. CLAUDE.md Conventions section is populated (not "not yet established")
2. CLAUDE.md Architecture section has folder structure including docker/
3. CLAUDE.md Key Commands table includes all pnpm scripts and Docker commands
4. CLAUDE.md Environment Variables section documents all vars from .env.example
5. All GSD comment markers are preserved
6. `pnpm run lint` still passes (CLAUDE.md is not linted, but verify no accidental code changes)
</verification>

<success_criteria>

- D-12 is fully satisfied: CLAUDE.md has been audited and updated with conventions, architecture, Docker commands, folder structure, and env var documentation
- No existing content was removed or reorganized — only gaps filled
- The file remains useful as the primary AI agent context document
  </success_criteria>

<output>
After completion, create `.planning/phases/08-production-docker/08-02-SUMMARY.md`
</output>
