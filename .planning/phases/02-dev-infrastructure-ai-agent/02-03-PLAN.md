---
phase: 02-dev-infrastructure-ai-agent
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE-DEV.md
  - .claude/settings.json
autonomous: true
requirements:
  - AI-01
  - AI-02

must_haves:
  truths:
    - 'CLAUDE-DEV.md exists at the project root with four required sections'
    - '.claude/settings.json contains mcpServers.svelte entry alongside existing GSD hooks'
    - 'Existing hooks and statusLine in .claude/settings.json are preserved exactly'
  artifacts:
    - path: 'CLAUDE-DEV.md'
      provides: 'Developer quick-reference for AI agents and new developers'
      contains: 'Key Commands'
    - path: '.claude/settings.json'
      provides: 'Claude Code configuration with Svelte MCP server'
      contains: 'mcpServers'
  key_links:
    - from: '.claude/settings.json'
      to: '@sveltejs/mcp'
      via: 'mcpServers.svelte.command = npx'
      pattern: '@sveltejs/mcp'
---

<objective>
Create the CLAUDE-DEV.md developer quick-reference and add the @sveltejs/mcp server to .claude/settings.json.

Purpose: Gives Claude Code (and human developers) a one-page cheat sheet for working in this repo, and enables Svelte/SvelteKit documentation access via MCP.
Output: CLAUDE-DEV.md at project root, updated .claude/settings.json with mcpServers block.
</objective>

<execution_context>
@/workspaces/sveltekit-starter/.claude/get-shit-done/workflows/execute-plan.md
@/workspaces/sveltekit-starter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dev-infrastructure-ai-agent/02-CONTEXT.md
@.planning/phases/02-dev-infrastructure-ai-agent/02-RESEARCH.md

<interfaces>
<!-- Current .claude/settings.json structure (must be preserved) -->
From .claude/settings.json:
```json
{
  "hooks": {
    "SessionStart": [...],
    "PostToolUse": [...],
    "PreToolUse": [...]
  },
  "statusLine": {
    "type": "command",
    "command": "node .claude/hooks/gsd-statusline.js"
  }
}
```
The `hooks` and `statusLine` keys MUST be preserved exactly. The `mcpServers` key is ADDED at the top level.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CLAUDE-DEV.md developer quick-reference</name>
  <files>CLAUDE-DEV.md</files>
  <read_first>
    - CLAUDE.md
    - .planning/phases/02-dev-infrastructure-ai-agent/02-CONTEXT.md
    - src/app.css
    - src/routes/+layout.svelte
  </read_first>
  <action>
Create `CLAUDE-DEV.md` at the project root per D-02. This is the developer quick-reference file — a one-page cheat sheet for AI agents and new developers. Claude Code reads all `CLAUDE*.md` files automatically (per D-01, do NOT modify the existing `CLAUDE.md`).

Per D-03, the file MUST contain exactly these four sections. Per D-04, do NOT list dependency versions — refer to `package.json`.

The file content:

```markdown
# Developer Quick Reference

> One-page cheat sheet for working in this repo. For the full tech stack reference, see `CLAUDE.md`.

## Key Commands

| Command                  | Description                                                                |
| ------------------------ | -------------------------------------------------------------------------- |
| `pnpm run dev`           | Start SvelteKit dev server (http://localhost:5173)                         |
| `pnpm run build`         | Production build via adapter-node (outputs to `build/`)                    |
| `pnpm run preview`       | Preview the production build locally                                       |
| `docker compose up`      | Start PostgreSQL + Aspire Dashboard dev stack                              |
| `docker compose down`    | Stop dev stack (data persists in named volume)                             |
| `docker compose down -v` | Stop dev stack AND reset database (removes volumes)                        |
| `pnpm run lint`          | Run ESLint _(available after Phase 3)_                                     |
| `pnpm run format`        | Run Prettier _(available after Phase 3)_                                   |
| `pnpm run check`         | Run svelte-check for type errors                                           |
| `pnpm test:unit`         | Run Vitest unit tests                                                      |
| `pnpm test:e2e`          | Run Playwright E2E tests _(available after Phase 7)_                       |
| `pnpm test`              | Run all tests (unit + E2E)                                                 |
| `pnpm run db:generate`   | Generate Drizzle migration from schema changes _(available after Phase 4)_ |
| `pnpm run db:migrate`    | Apply pending Drizzle migrations _(available after Phase 4)_               |
| `pnpm run db:push`       | Push schema directly to dev DB (no migration) _(available after Phase 4)_  |
| `pnpm run db:studio`     | Open Drizzle Studio GUI _(available after Phase 4)_                        |

## Folder Structure
```

src/
routes/ # SvelteKit file-based routing
+layout.svelte # Root layout (navbar + drawer + theme toggle)
+page.svelte # Home page
+error.svelte # Error page (DaisyUI styled)
api/ # API routes (added in later phases)
lib/
server/ # Server-only modules ($lib/server/\*)
env.ts # Zod-validated environment variables
db/ # Database client + schema (added in Phase 4)
assets/ # Static assets imported by components
components/ # Shared Svelte components
app.css # Tailwind v4 + DaisyUI CSS-first config
app.html # HTML shell with data-theme attribute
hooks.server.ts # Server hooks (env validation, security, OTEL)
drizzle/ # Generated migrations (added in Phase 4)
docker-compose.yml # Dev stack: PostgreSQL + Aspire Dashboard
.env.example # Environment variable template (copy to .env)
vite.config.ts # Vite config with Tailwind + SvelteKit + Vitest

```

## Coding Conventions

- **Svelte 5 runes** -- use `$state`, `$derived`, `$effect`, `$props` (not `writable()` stores)
- **Render children** -- use `{@render children()}` (not `<slot />`)
- **Dynamic env** -- use `$env/dynamic/private` for secrets (not `$env/static/private`); required for Docker runtime env
- **Server-only** -- put secrets, DB access, and sensitive logic in `$lib/server/`; SvelteKit enforces this at build time
- **CSS-first Tailwind** -- all Tailwind + DaisyUI config in `src/app.css` via `@import` and `@plugin`; no `tailwind.config.js`
- **Package manager** -- pnpm (not npm or yarn)
- **Types** -- TypeScript strict mode; prefer explicit types over `any`

## Testing Patterns

- **Unit tests**: `src/**/*.test.ts` -- run with `pnpm test:unit` (Vitest)
- **Component tests**: `src/**/*.svelte.test.ts` -- use `@testing-library/svelte` _(patterns established in Phase 7)_
- **E2E tests**: `tests/` directory -- run with `pnpm test:e2e` (Playwright) _(patterns established in Phase 7)_
- **Test environment**: Unit/server tests use `environment: 'node'`; component tests will use `jsdom`
- **Mocking SvelteKit modules**: Use `vi.mock('$env/dynamic/private', ...)` for env vars in tests
```

Per D-04: Note that no dependency versions are listed — the file says "see `CLAUDE.md`" for the full tech stack and `package.json` for versions.

Commands marked with "_(available after Phase N)_" indicate which phase fills them in, per the specific idea in CONTEXT.md.
</action>
<verify>
<automated>cd /workspaces/sveltekit-starter && test -f CLAUDE-DEV.md && grep -c "Key Commands" CLAUDE-DEV.md && grep -c "Folder Structure" CLAUDE-DEV.md && grep -c "Coding Conventions" CLAUDE-DEV.md && grep -c "Testing Patterns" CLAUDE-DEV.md</automated>
</verify>
<acceptance_criteria> - CLAUDE-DEV.md exists at the project root - CLAUDE-DEV.md contains `## Key Commands` section - CLAUDE-DEV.md contains `## Folder Structure` section - CLAUDE-DEV.md contains `## Coding Conventions` section - CLAUDE-DEV.md contains `## Testing Patterns` section - CLAUDE-DEV.md does NOT contain version numbers for dependencies (per D-04) - CLAUDE-DEV.md contains `pnpm run dev` in the commands table - CLAUDE-DEV.md contains `docker compose up` in the commands table - CLAUDE-DEV.md contains `$env/dynamic/private` in the conventions section - CLAUDE-DEV.md contains reference to `CLAUDE.md` for full tech stack
</acceptance_criteria>
<done>CLAUDE-DEV.md created with all 4 required sections; no version pins; commands marked TBD reference their future phase</done>
</task>

<task type="auto">
  <name>Task 2: Add @sveltejs/mcp to .claude/settings.json</name>
  <files>.claude/settings.json</files>
  <read_first>
    - .claude/settings.json
  </read_first>
  <action>
Read the existing `.claude/settings.json` and add the `mcpServers` key at the top level, preserving ALL existing `hooks` and `statusLine` content exactly as-is (per D-05).

The `mcpServers` block to add (per D-06):

```json
"mcpServers": {
  "svelte": {
    "command": "npx",
    "args": ["@sveltejs/mcp"]
  }
}
```

The final file structure should be:

```json
{
  "hooks": { ...existing hooks exactly as-is... },
  "statusLine": { ...existing statusLine exactly as-is... },
  "mcpServers": {
    "svelte": {
      "command": "npx",
      "args": ["@sveltejs/mcp"]
    }
  }
}
```

CRITICAL: Do NOT modify, reformat, or reorder any existing keys in hooks or statusLine. Read the file first, parse the JSON, add the mcpServers key, and write back. This preserves the GSD hook system.
</action>
<verify>
<automated>cd /workspaces/sveltekit-starter && node -e "const s = require('./.claude/settings.json'); console.log('hooks:', !!s.hooks, 'statusLine:', !!s.statusLine, 'mcpServers:', !!s.mcpServers, 'svelte:', !!s.mcpServers?.svelte, 'command:', s.mcpServers?.svelte?.command, 'args:', JSON.stringify(s.mcpServers?.svelte?.args))"</automated>
</verify>
<acceptance_criteria> - .claude/settings.json contains `"mcpServers"` key - .claude/settings.json contains `"svelte"` under mcpServers - .claude/settings.json mcpServers.svelte.command equals `"npx"` - .claude/settings.json mcpServers.svelte.args equals `["@sveltejs/mcp"]` - .claude/settings.json still contains `"hooks"` key with SessionStart, PostToolUse, PreToolUse - .claude/settings.json still contains `"statusLine"` key - JSON is valid (node -e require succeeds without error)
</acceptance_criteria>
<done>.claude/settings.json updated with mcpServers.svelte entry; all existing GSD hooks preserved</done>
</task>

</tasks>

<threat_model>

## Threat Model

| Threat                                           | STRIDE | Severity | Mitigation                                                                                                                                           |
| ------------------------------------------------ | ------ | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| No high/critical threats applicable to this plan | -      | -        | CLAUDE-DEV.md is documentation only; .claude/settings.json configures a local MCP server via npx. No secrets, no network exposure, no auth surfaces. |

</threat_model>

<verification>
1. CLAUDE-DEV.md has all 4 required sections (Key Commands, Folder Structure, Coding Conventions, Testing Patterns)
2. `.claude/settings.json` is valid JSON with hooks + statusLine + mcpServers keys
3. mcpServers.svelte points to `npx @sveltejs/mcp`
</verification>

<success_criteria>

- CLAUDE-DEV.md exists with all 4 sections per D-03, no version pins per D-04
- .claude/settings.json has mcpServers.svelte per D-06, existing hooks preserved per D-05
- CLAUDE.md is untouched per D-01
  </success_criteria>

<output>
After completion, create `.planning/phases/02-dev-infrastructure-ai-agent/02-03-SUMMARY.md`
</output>
