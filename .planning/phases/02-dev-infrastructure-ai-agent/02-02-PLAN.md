---
phase: 02-dev-infrastructure-ai-agent
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
autonomous: true
requirements:
  - DOCK-01
  - ENV-01

must_haves:
  truths:
    - 'docker compose up starts PostgreSQL 17 and Aspire Dashboard containers'
    - 'PostgreSQL health check passes within 60 seconds of starting'
    - 'Aspire Dashboard UI is reachable at http://localhost:18888'
    - '.env.example documents all required env vars with working dev defaults'
    - 'No hardcoded credentials exist in docker-compose.yml'
  artifacts:
    - path: 'docker-compose.yml'
      provides: 'Docker Compose dev stack with PostgreSQL and Aspire Dashboard'
      contains: 'postgres:17-alpine'
    - path: '.env.example'
      provides: 'Environment variable template with dev defaults'
      contains: 'DATABASE_URL'
  key_links:
    - from: 'docker-compose.yml'
      to: '.env.example'
      via: '${VAR:-default} substitution'
      pattern: "\\$\\{POSTGRES_"
---

<objective>
Create the Docker Compose dev stack with PostgreSQL 17 and .NET Aspire Dashboard, plus the .env.example template with documented dev defaults.

Purpose: Provides the running infrastructure (database + observability collector) that all subsequent phases depend on. A developer copies .env.example to .env and runs `docker compose up` to get a fully working local stack.
Output: docker-compose.yml and .env.example at the project root.
</objective>

<execution_context>
@/workspaces/sveltekit-starter/.claude/get-shit-done/workflows/execute-plan.md
@/workspaces/sveltekit-starter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dev-infrastructure-ai-agent/02-CONTEXT.md
@.planning/phases/02-dev-infrastructure-ai-agent/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docker-compose.yml with PostgreSQL and Aspire Dashboard</name>
  <files>docker-compose.yml</files>
  <read_first>
    - .gitignore
    - CLAUDE.md
  </read_first>
  <action>
Create `docker-compose.yml` at the project root with this exact content:

```yaml
services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-app}
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-app}']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  aspire-dashboard:
    image: mcr.microsoft.com/dotnet/aspire-dashboard:latest
    ports:
      - '18888:18888'
      - '4317:18889'
      - '4318:18890'
    environment:
      DASHBOARD__UNSECUREDALLOWEDLIST: '*'

volumes:
  postgres_data:
```

Key constraints per locked decisions:

- D-07: Two services — PostgreSQL 17 with named volume + pg_isready health check, Aspire Dashboard with ports 18888:18888, 4317:18889, 4318:18890
- D-08: All credentials use `${VAR:-default}` substitution — no hardcoded secrets
- No `version:` key — Docker Compose v2 does not require it and shows a deprecation warning if present
- `${VAR:-default}` syntax provides fallback defaults so `docker compose up` works even without a .env file (with dev defaults)
- `DASHBOARD__UNSECUREDALLOWEDLIST: '*'` disables the Aspire Dashboard login token for local dev convenience
- Port 5432 is exposed directly (no remapping) per D-10
  </action>
  <verify>
  <automated>cd /workspaces/sveltekit-starter && docker compose config --quiet 2>&1; echo "exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml exists at project root
    - docker-compose.yml contains `postgres:17-alpine`
    - docker-compose.yml contains `pg_isready`
    - docker-compose.yml contains `aspire-dashboard`
    - docker-compose.yml contains `18888:18888`
    - docker-compose.yml contains `4317:18889`
    - docker-compose.yml contains `4318:18890`
    - docker-compose.yml contains `${POSTGRES_PASSWORD:-postgres}`(not a hardcoded password)
    - docker-compose.yml contains`postgres_data:`in the volumes section
    -`docker compose config --quiet` exits with code 0
  </acceptance_criteria>
  <done>docker-compose.yml validates and defines PostgreSQL 17 + Aspire Dashboard with health checks and no hardcoded secrets</done>
  </task>

<task type="auto">
  <name>Task 2: Create .env.example with documented dev defaults</name>
  <files>.env.example</files>
  <read_first>
    - docker-compose.yml
    - .gitignore
  </read_first>
  <action>
Create `.env.example` at the project root with this exact content:

```bash
# .env.example
# Copy this file to .env for local development:
#   cp .env.example .env
#
# All defaults below work immediately with `docker compose up`.

# ──────────────────────────────────────────────
# PostgreSQL (used by docker-compose.yml and src/lib/server/env.ts)
# ──────────────────────────────────────────────
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=app

# Full connection URL consumed by the app (and later by Drizzle in Phase 4)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/app
```

Key constraints per locked decisions:

- D-09: Documents all Phase 2 required vars with inline comments and working dev defaults
- D-10: Default credentials are `postgres/postgres`, database `app`, port `5432`
- D-10: Connection string format: `postgres://postgres:postgres@localhost:5432/app`
- .gitignore already has `.env` and `!.env.example` entries, so .env.example will be committed but .env will not

Verify .gitignore: Confirm `.env` is in .gitignore and `!.env.example` is present (should already be there from SvelteKit scaffold). If `!.env.example` is missing, add it.
</action>
<verify>
<automated>cd /workspaces/sveltekit-starter && test -f .env.example && grep -c "DATABASE_URL" .env.example && grep -c "POSTGRES_USER" .env.example</automated>
</verify>
<acceptance_criteria> - .env.example exists at project root - .env.example contains `POSTGRES_USER=postgres` - .env.example contains `POSTGRES_PASSWORD=postgres` - .env.example contains `POSTGRES_DB=app` - .env.example contains `DATABASE_URL=postgres://postgres:postgres@localhost:5432/app` - .env.example contains `cp .env.example .env` instruction in comments - .gitignore contains `.env` (file is not committed) - .gitignore contains `!.env.example` (template IS committed)
</acceptance_criteria>
<done>.env.example created with all Phase 2 vars documented; developer can copy to .env for working dev stack</done>
</task>

</tasks>

<threat_model>

## Threat Model

| Threat                                                       | STRIDE          | Severity | Mitigation                                                                                                                                               |
| ------------------------------------------------------------ | --------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Hardcoded credentials in docker-compose.yml committed to git | Info Disclosure | high     | All credentials use `${VAR:-default}` substitution per D-08. Defaults are dev-only values (postgres/postgres) acceptable for local development.          |
| .env file with real credentials committed to git             | Info Disclosure | high     | .gitignore already contains `.env` and `.env.*` exclusions (verified). Only `.env.example` (with dev defaults) is committed via `!.env.example`.         |
| PostgreSQL exposed on 0.0.0.0:5432 to network                | Spoofing        | medium   | Dev-only concern. Docker default binding is to all interfaces. Acceptable for local dev starter; production Docker (Phase 8) will use internal networks. |
| Aspire Dashboard open without authentication                 | Info Disclosure | low      | Dashboard is for local dev only; `DASHBOARD__UNSECUREDALLOWEDLIST` is set for convenience. Not production-facing.                                        |

</threat_model>

<verification>
1. `docker compose config --quiet` exits 0 (valid compose file)
2. `grep -c 'hardcoded_password' docker-compose.yml` returns 0 (no hardcoded secrets)
3. `.env.example` contains all 4 required vars
4. `.gitignore` excludes `.env` but allows `.env.example`
</verification>

<success_criteria>

- docker-compose.yml defines PostgreSQL 17 + Aspire Dashboard with health checks
- No hardcoded credentials in docker-compose.yml
- .env.example documents all required vars with working dev defaults
- docker compose config validates successfully
  </success_criteria>

<output>
After completion, create `.planning/phases/02-dev-infrastructure-ai-agent/02-02-SUMMARY.md`
</output>
