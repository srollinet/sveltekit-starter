# Phase 4: Database — Discussion Log

**Date:** 2026-04-06
**Mode:** Interactive discuss-phase

---

## Areas Selected

User selected all three gray areas: Example table design, Migration strategy, Schema file structure.

---

## Area 1: Example table design

**Q: What should the demo table be called?**
Options: posts (Recommended), todos, example_items/items
→ **Selected: posts**

**Q: What columns should the example table include?**
Options: Minimal (id + one text + created_at), Moderate (id + title + body + timestamps), Rich (id + title + body + status enum + timestamps)
→ **Selected: Rich — id + title + body + status enum + timestamps**

**Q: What type should the primary key use?**
Options: UUID (Recommended), Serial/integer auto-increment
→ **Selected: UUIDv7** (user note: time-ordered UUID via `uuidv7` npm package)

---

## Area 2: Migration strategy

**Q: What should be the primary migration workflow for developers?**
Options: db:push for dev + db:generate+migrate for prod (Recommended), Always generate+migrate, db:push only
→ **Selected: db:push for dev, db:generate + db:migrate for prod (Recommended)**

**Q: Where should generated migration SQL files live?**
Options: drizzle/ at project root (Recommended), src/lib/server/db/migrations/
→ **Selected: drizzle/ at project root (Recommended)**

**Q: Should the first migration be generated and committed as part of Phase 4?**
Options: Yes (Recommended), No
→ **Selected: Yes — run db:generate, commit the SQL file**

**Q: Should db:migrate run automatically on startup?**
Options: Manual only (Recommended), Auto-migrate on startup in development
→ **Selected: Manual only — developer runs db:migrate explicitly**

---

## Area 3: Schema file structure

**Q: How should schema files be organized?**
Options: One file per domain area in schema/ directory (Recommended), Single schema.ts file
→ **Selected: One file per domain area in schema/ directory**

**Q: What should be in src/lib/server/db/index.ts?**
Options: postgres.js + drizzle() only (Recommended), postgres.js + drizzle() + schema re-exports, postgres.js + drizzle() + typed schema re-exports
→ **User note: "Check what got created when running `pnpx sv add drizzle`"**

_Claude note: Checked — `sv add drizzle` is the official Svelte CLI add-on. Command: `npx sv add drizzle="database:postgresql+client:postgres.js"`. Decision: use sv scaffolding as base, follow what it generates for db/index.ts, adjusted for schema/ directory structure._

**Q: Should Phase 4 restructure sv scaffold into schema/ directory?**
Options: Yes — restructure after scaffold (Recommended), Keep whatever sv add drizzle creates
→ **Selected: Yes — restructure into schema/ directory**

---

_Generated by /gsd-discuss-phase 4 on 2026-04-06_
