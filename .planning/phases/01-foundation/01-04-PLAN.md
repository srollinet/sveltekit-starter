---
phase: 01-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03-PLAN.md
files_modified: []
autonomous: false
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - FOUND-06
  - FOUND-07
must_haves:
  truths:
    - 'pnpm run dev starts successfully and serves the home page'
    - 'Home page shows styled DaisyUI card grid (not a blank page)'
    - 'Mobile navbar hamburger opens a drawer on narrow viewports'
    - 'Theme toggle switches between light and dark modes visibly'
    - 'Theme persists after page reload'
    - 'Navigating to /nonexistent shows styled DaisyUI error page'
    - 'pnpm run build produces a build/ directory'
  artifacts:
    - path: 'build/'
      provides: 'adapter-node production build output'
  key_links:
    - from: 'pnpm run build'
      to: 'build/index.js'
      via: 'adapter-node output'
      pattern: 'build/'
---

<objective>
Verify all 5 Phase 1 success criteria by running the app and performing visual + functional checks.

Purpose: Confirm that the implemented foundation is actually working from a user's perspective — styled pages, functional navigation, theme switching, and a passing build.

Output: Verified confirmation that all FOUND-01 through FOUND-07 requirements are met.
</objective>

<execution_context>
@/workspaces/sveltekit-starter/.claude/get-shit-done/workflows/execute-plan.md
@/workspaces/sveltekit-starter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated build verification</name>
  <files></files>
  <action>
Run all automated checks to confirm the build and type-checking pass before visual verification.

**Step 1 — TypeScript check:**

```bash
cd /workspaces/sveltekit-starter
pnpm run check
```

Must complete with zero errors. If errors exist, fix them before proceeding.

**Step 2 — Production build:**

```bash
cd /workspaces/sveltekit-starter
pnpm run build
```

Must succeed and produce a `build/` directory. This verifies FOUND-04 (adapter-node build output).

**Step 3 — Verify build output:**

```bash
ls /workspaces/sveltekit-starter/build/
```

The `build/` directory must exist and contain `index.js` (the adapter-node entry point).

**Step 4 — Verify forbidden patterns are absent:**

```bash
# No adapter-auto
grep -r "adapter-auto" /workspaces/sveltekit-starter/svelte.config.js && echo "FAIL: adapter-auto found" || echo "PASS: no adapter-auto"

# No tailwind.config.js
test -f /workspaces/sveltekit-starter/tailwind.config.js && echo "FAIL: tailwind.config.js exists" || echo "PASS: no tailwind.config.js"

# No @tailwind directives
grep -r "@tailwind" /workspaces/sveltekit-starter/src/ && echo "FAIL: @tailwind directive found" || echo "PASS: no @tailwind directives"

# No .eslintrc files
test -f /workspaces/sveltekit-starter/.eslintrc.js && echo "FAIL: .eslintrc.js found" || echo "PASS: no .eslintrc.js"
test -f /workspaces/sveltekit-starter/.eslintrc.json && echo "FAIL: .eslintrc.json found" || echo "PASS: no .eslintrc.json"
```

All checks must print PASS.
</action>
<verify>
<automated>cd /workspaces/sveltekit-starter && pnpm run check 2>&1 | tail -3 && pnpm run build 2>&1 | tail -5 && test -d build && echo "BUILD_OUTPUT: OK"</automated>
</verify>
<done>pnpm run check passes with zero errors; pnpm run build succeeds; build/ directory exists with index.js; all forbidden pattern checks return PASS.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 1 Foundation:
    - SvelteKit 2 + Svelte 5 (runes) scaffold with TypeScript strict mode (FOUND-01)
    - Tailwind CSS v4 CSS-first configuration with @import "tailwindcss" (FOUND-02)
    - DaisyUI v5 configured via @plugin "daisyui" with light + dark themes (FOUND-03)
    - adapter-node configured in svelte.config.js (FOUND-04)
    - Root layout: DaisyUI top navbar with mobile hamburger drawer + theme toggle (FOUND-05)
    - Home page: stack showcase card grid (8 items, DaisyUI cards) (FOUND-06)
    - Error page: DaisyUI styled 404 with status code and "Back to home" button (FOUND-07)
  </what-built>
  <how-to-verify>
    Run the dev server:
    ```
    cd /workspaces/sveltekit-starter
    pnpm run dev
    ```
    Then verify these 5 success criteria:

    **Criterion 1 — Dev server starts and serves home page:**
    Open http://localhost:5173 in a browser. You should see a page titled "SvelteKit Battery-Included Starter" with a card grid.

    **Criterion 2 — Styled DaisyUI components visible:**
    The home page must show 8 cards with colored badges (Primary, Secondary, Accent, etc.), not plain unstyled text. Cards should have shadows and rounded corners.

    **Criterion 3 — Mobile-responsive navigation:**
    Resize browser to narrow width (< 640px) or use DevTools mobile emulation. The navbar should show a hamburger (☰) button. Clicking it should open a drawer/sidebar with the Home link.

    **Criterion 4 — Theme toggle works and persists:**
    Click the sun/moon icon in the top-right of the navbar. The page should switch between light mode (white background) and dark mode (dark background). Reload the page — the theme should be remembered.

    **Criterion 5 — Styled error page:**
    Navigate to http://localhost:5173/this-does-not-exist. You should see a styled error page showing "404" and "Page not found" with DaisyUI styling (not SvelteKit's default unstyled error). It should have a "Back to home" button.

  </how-to-verify>
  <resume-signal>Type "approved" if all 5 criteria pass, or describe which criteria failed and what you saw instead.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                         |
| ---------- | --------------------------------------------------- |
| dev server | Local-only; no external traffic during verification |

## STRIDE Threat Register

| Threat ID | Category               | Component        | Disposition | Mitigation Plan                                                 |
| --------- | ---------------------- | ---------------- | ----------- | --------------------------------------------------------------- |
| T-04-01   | Information Disclosure | build/ artifacts | accept      | build/ output is local dev artifact; not deployed in this phase |

</threat_model>

<verification>
The checkpoint task IS the verification for this plan. All 5 phase success criteria are verified manually:
1. `pnpm run dev` starts and serves home page at http://localhost:5173
2. Home page renders styled DaisyUI card grid
3. Mobile-responsive navigation with hamburger drawer
4. Theme toggle switches light/dark and persists to localStorage
5. /nonexistent shows styled DaisyUI error page
</verification>

<success_criteria>
All 5 Phase 1 success criteria confirmed by human:

1. pnpm run dev starts the SvelteKit app and serves the home page
2. Home page renders styled DaisyUI components
3. Mobile-responsive navigation is visible and functional on narrow viewports
4. Navigating to a nonexistent route shows a styled error page (not default SvelteKit error)
5. pnpm run build succeeds using adapter-node and produces a build/ directory
   </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md` with:
- Confirmation of all 5 success criteria (pass/fail)
- Any issues found during visual verification and how they were fixed
- Final state of all phase files

Also update `.planning/ROADMAP.md`:

- Change `- [ ] **Phase 1: Foundation**` to `- [x] **Phase 1: Foundation**`
- Update the progress table: Phase 1 row → `4/4` plans complete, `Done`, today's date
  </output>
