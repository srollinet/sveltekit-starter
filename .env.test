# Test environment defaults — safe to commit (no real secrets).
# Used by vitest (NODE_ENV=test) and playwright (vite preview --mode test).

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=app

DATABASE_URL=postgres://postgres:postgres@localhost:5432/app
